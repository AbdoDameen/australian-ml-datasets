# Number of dwelling units approved, by sector, all series - New South Wales

## Overview
This folder contains the dataset "Number of dwelling units approved, by sector, all series - New South Wales" provided as an Excel file. The notebook in this folder will:

- Move the original Excel file into `raw/` (if not already there).
- Clean and standardize the data.
- Prepare ML-ready features (encode categoricals, scale numerics).
- Save cleaned data to `processed/` and ML-ready features to `features/`.
- Update `metadata.json` with processing details.

## Files
- `Number of dwelling units approved, by sector, all series - New South Wales.xlsx` — original Excel file (moved to `raw/` by the notebook).
- `Number_of_dwelling_units_approved,_by_sector,_all_series_New_South_Wales.ipynb` — processing notebook (this file).
- `metadata.json` — dataset metadata and processing summary.
- `raw/` — raw originals (created by notebook).
- `processed/` — cleaned CSV output (created by notebook).
- `features/` — ML-ready feature files and scalers (created by notebook).

## How to run
1. Open the notebook and run all cells. The notebook will create `raw/`, `processed/`, and `features/` directories and save outputs there.
2. Place any updated original Excel file in the folder root before running; the notebook will move/copy it into `raw/`.

## Notes
- The notebook performs general-purpose cleaning and basic ML preparation; adjust encoding/scaling choices if you have a specific modeling target in mind.
- If you want a train/test split saved, update the `target_column` variable within the notebook to point to the desired target.

_Generated by the repository processing workflow._
